# ConvertTo-TomlFromXml

## Synopsis

Converts XML file to TOML format.

## Description

Converts an XML file to TOML (Tom's Obvious, Minimal Language) format.

## Signature

```powershell
ConvertTo-TomlFromXml
```

## Parameters

### -InputPath

The path to the XML file.

### -OutputPath

The path for the output TOML file. If not specified, uses input path with .toml extension.


## Examples

No examples provided.

## Aliases

This function has the following aliases:

- `xml-to-toml` - Converts XML file to TOML format.


## Source

Defined in: ../profile.d/conversion-modules/data/structured/toml.ps1
