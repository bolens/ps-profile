# ConvertTo-ToonFromYaml

## Synopsis

Converts YAML file to TOON format.

## Description

Converts a YAML file to TOON (Token-Oriented Object Notation) format.

## Signature

```powershell
ConvertTo-ToonFromYaml
```

## Parameters

### -InputPath

The path to the YAML file.

### -OutputPath

The path for the output TOON file. If not specified, uses input path with .toon extension.


## Examples

No examples provided.

## Aliases

This function has the following aliases:

- `yaml-to-toon` - Converts YAML file to TOON format.


## Source

Defined in: ../profile.d/conversion-modules/data/structured/toon.ps1
