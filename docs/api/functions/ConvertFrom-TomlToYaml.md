# ConvertFrom-TomlToYaml

## Synopsis

Converts TOML file to YAML format.

## Description

Converts a TOML (Tom's Obvious, Minimal Language) file to YAML format using yq.

## Signature

```powershell
ConvertFrom-TomlToYaml
```

## Parameters

### -InputPath

The path to the TOML file.

### -OutputPath

The path for the output YAML file. If not specified, uses input path with .yaml extension.


## Examples

No examples provided.

## Aliases

This function has the following aliases:

- `toml-to-yaml` - Converts TOML file to YAML format.


## Source

Defined in: ../profile.d/conversion-modules/data/structured/toml.ps1
