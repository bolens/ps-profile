# ConvertFrom-Utf16ToBase64

## Synopsis

Treat input as UTF-16 hex, decode to text, then encode as UTF-8 Base64

## Description

Treat input as UTF-16 hex, decode to text, then encode as UTF-8 Base64

## Signature

```powershell
ConvertFrom-Utf16ToBase64 [String]$InputObject
```

## Parameters

No parameters.

## Examples

No examples provided.

## Aliases

This function has the following aliases:

- `utf16-to-base64` - Treat input as UTF-16 hex, decode to text, then encode as UTF-8 Base64


## Source

Defined in: ../profile.d/conversion-modules/data/encoding/utf16-utf32.ps1
