# ConvertTo-JsonLFromJson

## Synopsis

Converts JSON file to JSONL format.

## Description

Converts a JSON file (array or object) to JSONL (JSON Lines) format, with one JSON object per line.

## Signature

```powershell
ConvertTo-JsonLFromJson
```

## Parameters

### -InputPath

The path to the JSON file.

### -OutputPath

The path for the output JSONL file. If not specified, uses input path with .jsonl extension.


## Examples

No examples provided.

## Aliases

This function has the following aliases:

- `json-to-jsonl` - Converts JSON file to JSONL format.


## Source

Defined in: ../profile.d/conversion-modules/data/core/json-extended.ps1
