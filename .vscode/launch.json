{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug current PowerShell script",
      "type": "PowerShell",
      "request": "launch",
      "script": "${file}",
      "cwd": "${workspaceFolder}",
      "createTemporaryIntegratedConsole": true
    }
  ]
}
